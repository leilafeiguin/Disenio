@using Diseño.Models
@model IEnumerable<Diseño.Models.Indicador>

@{
    ViewBag.Title = "Indicadores";
}

<h2>Indicadores</h2>

<p>
    @Html.ActionLink("Crear nueva cuenta", "Create")
</p>
    
<body>
    @using (Html.BeginForm("Index", "Indicadores", FormMethod.Post, new { enctype = "multipart/form-data" }))

     @if (Model.Count() > 0)
     {
         <hr />
         <table cellpadding="0" cellspacing="0">
             <tr>
                 <th>ID</th>
                 <th>Nombre</th>
                 <th>Formula</th>
             </tr>
             @foreach (Indicador indicador in Model)
             {
                 <tr>
                     <td>@cuenta.ID</td>
                     <td>@cuenta.Nombre</td>
                     <td>@cuenta.Formula</td>
                 </tr>
             }
         </table>
     }
</body>  


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Formula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Formula)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ID }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>